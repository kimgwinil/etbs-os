# 이제너두 전사 CRM OS DevOps A9 초안

## 1. 문서 목적

이 문서는 이제너두 전사 CRM OS MVP의 CI/CD, 환경 분리, 시크릿 관리, 로그/메트릭/알림, 운영 KPI 관측성 기준을 정의하는 A9 DevOps 초안이다.

본 문서는 확정안이 아니며 특정 클라우드, 상용 관측성 도구, 배포 플랫폼, 보안 솔루션, 외부 계약은 PM 김권일 승인 후 확정한다.

인당 생산성/마진 기여: 개발, 검증, 배포, 장애 대응을 표준화해 반복 작업과 장애 복구 시간을 줄이고 동일 인력의 릴리즈 처리량을 높인다.

## 2. 기준 입력

| 입력 | 내용 | 상태 |
|---|---|---|
| PRD | `/docs/PRD.md` | 확인 완료 |
| 프로세스 요구사항 | `/docs/PROCESS_REQUIREMENTS.md` | 확인 완료 |
| 타임로그/감사로그 스펙 | `/specs/audit/timelog-auditlog.md` | 확인 완료 |
| 수익성 비용 입력 권한 스펙 | `/specs/settings/profit-cost-permission.md` | 확인 완료 |
| MVP P0 모듈 | CRM Core, Operations Console, Security & Audit Baseline, Profitability Dashboard | 기준 입력 |
| 규제 요구 | ISMS, 개인정보, 외감 대응을 위한 로그와 배포 이력 추적 | 기준 입력 |
| 미확정 항목 | 클라우드, 상용도구, 개인정보 처리범위, 외부계약, 최종 아키텍처 | PM 승인 필요 |

인당 생산성/마진 기여: DevOps 기준을 P0 범위에 맞춰 제한해 초기 구축 비용을 통제하고 고위험 운영 병목부터 줄인다.

## 3. A9 담당 범위

### 포함

- MVP CI/CD 파이프라인 기준
- 한 명령 빌드/테스트/배포 기준
- 개발, 검증, 운영 환경 분리 기준
- 시크릿 수명주기와 접근 통제 기준
- 로그, 메트릭, 트레이스, 알림 기준
- 타임로그/감사로그 보존과 외감 대응 증적 기준
- 배포 이력, 변경 이력, 감사 증적 기준
- 운영 KPI와 시스템 관측성 연결 기준
- 배송/입출고 및 외부 연동 실패 알림 기준

### 제외

- 특정 클라우드 또는 SaaS 도구 확정
- 개인정보 항목, 보관기간, 위탁 범위 확정
- 최종 네트워크, 데이터베이스, 런타임 아키텍처 확정
- 예산 또는 외부계약 확정
- 제품 기능 구현 코드 작성

인당 생산성/마진 기여: A9 범위를 운영 자동화와 증적 기준으로 한정해 개발 에이전트가 같은 기준으로 빠르게 통합되도록 한다.

## 4. MVP DevOps 원칙

| 원칙 | 기준 | 인당 생산성/마진 기여 |
|---|---|---|
| One Command Delivery | 로컬과 CI에서 동일한 명령으로 빌드, 테스트, 배포가 가능해야 한다. | 환경 차이로 인한 디버깅과 배포 대기 시간을 줄인다. |
| Environment Parity | dev, staging, prod는 설정값만 다르고 실행 방식은 동일해야 한다. | 운영 전환 재작업과 장애 가능성을 줄인다. |
| Secret Zero Trust | 시크릿은 코드, 문서, 이미지, 로그에 저장하지 않는다. | 유출 사고와 감사 대응 비용을 줄인다. |
| Audit by Default | 배포, 권한, 개인정보 접근, 주요 데이터 변경은 추적 가능해야 한다. | 외감/ISMS 대응 시간을 줄이고 사후 복구 비용을 낮춘다. |
| Observable Operations | 업무 KPI와 시스템 지표를 같은 시간축으로 조회해야 한다. | 장애가 매출/마진/처리량에 미치는 영향을 빠르게 판단한다. |
| Fail Fast, Roll Back Fast | 배포 전 자동 검증과 배포 후 빠른 롤백 경로를 갖춘다. | 장애 확산 시간을 줄여 운영 인력 투입과 손실을 줄인다. |

## 5. 환경 분리 기준

| 환경 | 목적 | 데이터 기준 | 접근 기준 | 배포 기준 |
|---|---|---|---|---|
| local | 개발자 단위 개발과 빠른 검증 | 실데이터 금지, 샘플/마스킹 데이터만 허용 | 개발자 개인 | 수동 실행 |
| dev | 통합 개발 검증 | 비식별 테스트 데이터 | 개발/QA | main 병합 전후 자동 배포 가능 |
| staging | 운영 전 최종 검증, UAT, 회귀 테스트 | 운영 유사 데이터는 비식별/마스킹 후 사용 | 제한된 개발/운영/보안 담당 | 승인 기반 배포 |
| prod | 실제 운영 | 승인된 개인정보 처리범위 내 데이터 | 최소권한, Break Glass 별도 | 승인 기반 배포와 롤백 필수 |

결정: MVP는 최소 `local`, `dev`, `staging`, `prod` 4단계 환경을 기준으로 문서화한다.

인당 생산성/마진 기여: 환경별 용도를 분리해 운영 장애와 데이터 사고 가능성을 낮추고 재작업 시간을 줄인다.

PM 승인 필요: 운영 유사 데이터의 마스킹 방식, 개인정보 처리범위, 프로덕션 접근자 범위.

## 6. 한 명령 빌드/테스트/배포 기준

MVP 저장소는 다음 명령 인터페이스를 제공해야 한다. 실제 구현 도구는 아키텍처 확정 후 선택한다.

| 명령 | 목적 | 필수 동작 | 실패 조건 |
|---|---|---|---|
| `make setup` | 로컬 개발 환경 준비 | 의존성 설치, 샘플 설정 파일 생성, 헬스체크 | 필수 도구 누락, 설정 파일 오류 |
| `make lint` | 정적 검증 | 포맷, 린트, 타입 체크 | 경고 기준 초과 또는 오류 |
| `make test` | 자동 테스트 | 단위 테스트, 계약 테스트, 핵심 권한 테스트 | 테스트 실패, 커버리지 기준 미달 |
| `make build` | 배포 산출물 생성 | 프론트엔드/백엔드 빌드, 이미지 또는 아티팩트 생성 | 빌드 실패, 취약점 기준 초과 |
| `make migrate` | 스키마 변경 검증/적용 | 마이그레이션 검증, 롤백 스크립트 확인 | 역방향 호환성 실패 |
| `make deploy ENV=dev` | 개발 환경 배포 | dev 배포, smoke test | 헬스체크 실패 |
| `make deploy ENV=staging` | 검증 환경 배포 | 승인된 아티팩트 배포, 회귀 smoke test | 승인 누락, smoke test 실패 |
| `make deploy ENV=prod` | 운영 배포 | 승인, 배포, 헬스체크, 변경 로그 기록 | 승인 누락, SLO 위험 |
| `make rollback ENV=prod VERSION=<version>` | 운영 롤백 | 직전 안정 버전 복구, 롤백 로그 기록 | 복구 검증 실패 |

결정: MVP는 도구가 달라도 위 명령 계약을 유지한다.

인당 생산성/마진 기여: 담당자가 도구별 절차를 외우지 않아도 동일 명령으로 검증과 배포를 수행해 대기와 실수를 줄인다.

PM 승인 필요: 최종 빌드 도구, 컨테이너 레지스트리, 배포 런타임, 배포 승인 체계.

## 7. CI/CD 파이프라인 기준

### Pull Request 검증

| 단계 | 필수 검증 |
|---|---|
| checkout | 의존성 캐시와 잠금 파일 무결성 확인 |
| lint | 포맷, 린트, 타입 체크 |
| test | 단위 테스트, 권한 테스트, API 계약 테스트 |
| build | 배포 산출물 생성 |
| security scan | dependency, secret, container/image 취약점 검사 |
| migration check | 스키마 변경의 적용/롤백 가능성 확인 |
| audit preview | 변경 대상 모듈과 감사로그 영향 표시 |

### main 병합 후

| 단계 | 필수 동작 |
|---|---|
| artifact publish | 버전 태그, 커밋 SHA, 빌드 시간, 빌드 주체 기록 |
| deploy dev | 자동 배포 및 smoke test |
| deploy staging | 승인 후 배포, 회귀 smoke test |
| release note | 변경 이력, 마이그레이션, 롤백 지침 생성 |

### prod 배포

| 단계 | 필수 동작 |
|---|---|
| approval gate | PM 또는 위임된 릴리즈 책임자 승인 |
| change window | 업무 피크 시간 회피 기준 확인 |
| canary/blue-green | 점진 배포 또는 즉시 롤백 가능한 방식 사용 |
| smoke test | 인증, 권한, 핵심 조회, 운영 큐, 영업이익 KPI 대시보드, 감사로그 기록 검증 |
| monitoring window | 배포 후 최소 30~60분 핵심 지표, 영업이익 KPI 갱신, 배송/연동 실패율 감시 |
| rollback decision | 오류율, 지연, 업무 실패율 기준 초과 시 롤백 |

결정: MVP부터 배포 이력은 커밋 SHA, 아티팩트 버전, 승인자, 배포자, 배포 시간, 롤백 가능 버전을 기록한다.

인당 생산성/마진 기여: 배포 추적성을 확보해 장애 원인 파악과 외감 증적 준비 시간을 줄인다.

PM 승인 필요: 배포 승인자, 업무 피크 시간, 운영 릴리즈 캘린더, 최종 CI/CD 플랫폼.

## 8. 시크릿 관리 기준

| 영역 | 기준 |
|---|---|
| 저장 | 시크릿은 코드 저장소, `.env` 커밋, 문서, 이슈, 로그에 저장 금지 |
| 주입 | 런타임 또는 CI/CD의 Secret Store에서 환경변수 또는 파일 마운트로 주입 |
| 권한 | 환경별, 역할별 최소권한 부여 |
| 회전 | 외부 API 키, DB 비밀번호, 토큰은 정기 회전 및 사고 시 즉시 회전 |
| 감사 | 조회, 변경, 회전, 삭제 이력을 보존 |
| 스캔 | PR과 main 브랜치에서 secret scan 필수 |
| 노출 대응 | 노출 확인 즉시 폐기, 재발급, 영향 범위 기록, PM/보안 담당 보고 |

시크릿 분류:

| 등급 | 예시 | 처리 기준 |
|---|---|---|
| S1 | 로컬 샘플 키, 더미 토큰 | 실제 시스템 접근 불가 |
| S2 | dev/staging API 키 | 환경 한정, 자동 회전 권장 |
| S3 | prod DB, 결제, 베네카페, 온누리, 인증 키 | 최소권한, 접근 승인, 감사로그 필수 |
| S4 | 개인정보 복호화 키, 마스터 키 | 별도 보안 설계 및 PM 승인 필요 |

결정: MVP는 `Secret Store` 추상 인터페이스를 기준으로 작성하고 특정 제품명은 확정하지 않는다.

인당 생산성/마진 기여: 시크릿 유출로 인한 서비스 중단, 고객 신뢰 손실, 감사 대응 비용을 예방한다.

PM 승인 필요: Secret Store 제품, 키 관리 방식, 회전 주기, S4 키 운영 책임자.

## 9. 로그 기준

### 공통 로그 필드

| 필드 | 설명 |
|---|---|
| `timestamp` | ISO 8601 UTC 기준 |
| `event_time` | 이벤트 발생 시각 |
| `log_id` | 타임로그 또는 감사로그 고유 ID |
| `log_type` | `timelog`, `auditlog`, `privacy_access_log` |
| `environment` | local, dev, staging, prod |
| `service` | 서비스 이름 |
| `version` | 배포 버전 또는 커밋 SHA |
| `trace_id` | 요청 추적 ID |
| `request_id` | 외부 요청 ID |
| `actor_id` | 사용자 또는 시스템 행위자 ID |
| `actor_user_id` | 사용자 행위자 ID |
| `actor_role` | 행위자 권한 |
| `tenant_id` | 고객사/테넌트 ID |
| `entity_type` | customer, supplier, employee, contract, operation_task 등 |
| `entity_id` | 대상 엔티티 ID |
| `object_type` | 대상 객체 유형 |
| `object_id` | 대상 객체 ID |
| `menu_id` | 메뉴별 로그 식별자 |
| `menu_name` | 메뉴 표시명 |
| `route_id` | 화면 또는 라우트 식별자 |
| `action` | 조회, 생성, 변경, 삭제, 승인, 반려, 배포 등 |
| `result` | success, failure, denied, timeout |
| `duration_ms` | 처리 시간 |
| `error_code` | 표준 오류 코드 |
| `pii_accessed` | 개인정보 접근 여부 |
| `before_ref` | 변경 전 값, 해시, 스냅샷 참조 중 하나 |
| `after_ref` | 변경 후 값, 해시, 스냅샷 참조 중 하나 |
| `approval_id` | 승인 기반 이벤트의 승인 ID |
| `cost_input_type` | 제원원가, 판관비, 일반관리비 구분 |
| `cost_input_value` | 비용 입력값 또는 비율 후보 |
| `formula_version` | 영업이익 산식 버전 |

### 감사로그 필수 이벤트

| 이벤트 | 필수 기록 |
|---|---|
| 로그인/로그아웃 | actor, IP, user agent, 성공/실패 |
| 권한 변경 | 변경자, 대상자, 이전 권한, 이후 권한, 사유 |
| 설정 변경 | 변경자, 설정 키, 이전 값 요약, 이후 값 요약, 승인자, 사유 |
| 개인정보 조회 | actor, tenant, entity, 목적, 결과 |
| 운영 업무 상태 변경 | 이전 상태, 이후 상태, 담당자, 사유 |
| 영업 프로세스 상태 변경 | 영업활동, 파이프라인, 견적, 계약, 주문, 출고, 배송, 발행, 매출인식의 이전 상태와 이후 상태 |
| 결재 문건/결재 라인 변경 | 문건 ID, 결재자 순서, 이전 라인, 이후 라인, 승인/반려 사유 |
| 정산/포인트 관련 변경 | 금액, 이전 값, 이후 값, 승인자, 근거 |
| 영업이익 산출 기준 변경 | 변경자, 이전 산식/비율, 이후 산식/비율, 승인자, 적용 시점 |
| 수익성 비용 입력 | `cost_input_type`, `formula_version`, `actor_role`, `before_ref`, `after_ref`, `approval_id`, `result` |
| 비용 입력 실패/차단 | 권한 없는 접근/저장 시도, 차단 사유, 행위자 권한, 결과 |
| 배송/입출고 예외 | 주문, 송장번호, 택배사, 실패 유형, 재조회/수동보정 결과 |
| 데이터 반입/반출 | 파일명, 범위, 건수, 요청자, 승인자 |
| 메뉴별 로그 | `menu_id`, `menu_name`, `route_id`, `action`, `result`, `actor_role` |
| 로그 조회/상세 조회 | 조회자, 조회 범위, 필터, 대상 로그 ID, 결과 |
| 입사등록 결재 | `onboarding_id`, `approval_id`, `employee_id`, 상신자, 현재 결재자, 승인/반려/보류 상태 |
| 배포/롤백 | 버전, 승인자, 실행자, 결과 |

결정: 애플리케이션 로그와 감사로그는 목적을 분리하되 `trace_id`, `actor_id`, `tenant_id`, `entity_id`로 연결 가능해야 한다.

인당 생산성/마진 기여: 운영 문의, 장애, 감사 요청에서 원인 추적 시간을 줄여 고숙련 인력의 반복 조사를 줄인다.

PM 승인 필요: 로그 보존기간, 개인정보 로그 마스킹 정책, 외감 증적 보존 기준.

### 타임로그/감사로그 보존 기준

| 대상 | 로그 유형 | 보존 요구 |
|---|---|---|
| 고객정보, 임직원정보, 상품정보 | 생성, 수정, 삭제, 조회, 권한 거부 | 보존기간 PM 승인 필요 |
| 영업활동, 파이프라인, 견적, 계약 | 생성, 상태 변경, 승인, 버전 변경 | 보존기간 PM 승인 필요 |
| 주문, 출고, 배송, 세금계산서, 거래명세표 | 생성, 발행, 상태 변경, 외부 전송, 실패/재처리 | 보존기간 PM 승인 필요 |
| 매출정보, 매출인식, 영업이익 산출 기준 | 생성, 조정, 산식/비율 변경, 승인 | 보존기간 PM 승인 필요 |
| 수익성 비용 입력 | 제원원가, 판관비, 일반관리비 입력/수정/삭제, 적용일 변경, 권한 차단 | 보존기간 PM 승인 필요 |
| 결재 문건 및 결재 라인 | 작성, 상신, 결재자 변경, 승인, 반려 | 보존기간 PM 승인 필요 |
| 권한/설정 변경 | 권한 부여/회수, 메뉴 노출, 기능 플래그, KPI 산식 설정 변경 | 보존기간 PM 승인 필요 |
| 배포 이력과 롤백 이력 | 아티팩트, 커밋 SHA, 승인자, 실행자, 결과, 롤백 가능 버전 | 외감 대응 증적으로 보존, 기간 PM 승인 필요 |

결정: 핵심 업무 데이터의 타임로그와 내부통제 감사로그는 분리 저장하되 동일 이벤트 ID로 상호 참조한다.

인당 생산성/마진 기여: 업무 이력과 통제 이력을 동시에 추적해 원인 조사와 외감 증적 취합 시간을 줄인다.

PM 승인 필요: 로그 저장소, WORM 또는 불변 저장 방식, 보존기간, 삭제/파기 정책, 상용 로그 보관 도구.

### 로그 보존/무결성 기준

| 통제 | 기준 |
|---|---|
| 보존 | 비용 입력, 산식 버전 변경, 권한 차단 로그는 감사로그 보존 대상에 포함한다. |
| 무결성 | `before_ref`, `after_ref`, `formula_version`, `approval_id`는 변경 감지 가능한 방식으로 보존한다. |
| 조회 감사 | 수익성 비용 입력 로그의 목록 조회, 상세 조회, 반출 요청 자체도 감사로그로 남긴다. |
| 반출 통제 | 비용 입력 로그 반출은 요청자, 승인자, 목적, 범위, 건수, 결과를 기록한다. |
| 마스킹 | 개인정보가 포함된 행위자 정보는 권한별 마스킹 정책을 적용한다. |

결정: 수익성 비용 입력 로그는 영업이익 산식 신뢰도에 직접 영향을 주므로 일반 설정 로그가 아니라 감사로그 보존 대상으로 둔다.

인당 생산성/마진 기여: 잘못된 비용 입력과 임의 산식 변경을 빠르게 추적해 경영 지표 재검산 시간을 줄인다.

PM 승인 필요: 비용 입력 로그 보존기간, 무결성 검증 방식, 로그 반출 권한, 상용 증적 관리 도구.

## 10. 메트릭 기준

### 시스템 메트릭

| 메트릭 | 기준 |
|---|---|
| `request_rate` | 서비스별 요청량 |
| `error_rate` | 4xx/5xx 및 업무 오류율 |
| `latency_p50/p95/p99` | API와 주요 쿼리 지연 |
| `availability` | 서비스 가용성 |
| `job_success_rate` | 배치/연동 작업 성공률 |
| `queue_depth` | 운영 큐, 연동 큐, 재처리 큐 적체량 |
| `retry_count` | 외부 연동 재시도 횟수 |
| `delivery_failure_rate` | 배송 지연, 오배송, 반송, 미출고 등 배송 예외율 |
| `carrier_integration_failure_rate` | 택배사/배송 API 연동 실패율 |
| `deployment_frequency` | 배포 빈도 |
| `change_failure_rate` | 배포 후 장애 또는 롤백 비율 |
| `mean_time_to_recovery` | 장애 복구 시간 |

### 보안/감사 메트릭

| 메트릭 | 기준 |
|---|---|
| `permission_denied_rate` | 권한 거부 비율 |
| `pii_access_count` | 개인정보 조회 건수 |
| `privileged_access_count` | 관리자 권한 사용 건수 |
| `secret_rotation_age` | 시크릿 마지막 회전 후 경과일 |
| `audit_log_delivery_lag` | 감사로그 저장 지연 |
| `permission_change_count` | 권한 변경 건수 |
| `settings_change_count` | 설정 변경 건수 |
| `cost_input_change_count` | 제원원가, 판관비, 일반관리비 입력/수정/삭제 건수 |
| `cost_input_denied_count` | 비용 입력 권한 없는 접근 또는 저장 차단 건수 |
| `cost_input_failure_count` | 비용 입력 저장 실패 건수 |
| `formula_version_change_count` | 산식 버전 생성 또는 변경 건수 |
| `menu_log_event_count` | 메뉴별 로그 이벤트 건수 |
| `log_view_count` | 로그 목록/상세 조회 건수 |
| `log_export_request_count` | 로그 반출 요청 건수 |
| `onboarding_approval_event_count` | 입사등록 결재 이벤트 건수 |
| `onboarding_approval_lead_time_ms` | 입사등록 상신부터 최종 승인까지 소요 시간 |

### 영업이익 KPI 메트릭

| 메트릭 | 기준 |
|---|---|
| `sales_amount` | 매출금액 |
| `cost_amount` | MVP 기준 원가, 기본값은 `sales_amount * 0.5` |
| `sgna_amount` | MVP 기준 판매관리비 및 일반관리비, 기본값은 `sales_amount * 0.5` |
| `operating_profit` | `sales_amount - cost_amount - sgna_amount` |
| `operating_profit_rate` | `operating_profit / sales_amount` |
| `discount_amount` | 이익 저하 원인 후보 중 할인 금액 |
| `return_amount` | 이익 저하 원인 후보 중 반품 금액 |
| `delivery_cost_amount` | 배송비 또는 배송 예외 비용 |
| `exception_handling_time_ms` | 예외 처리 투입 시간 |
| `approval_delay_ms` | 결재 지연 시간 |
| `cost_input_value` | 비용 입력값 또는 비율 후보 |
| `formula_version_change_count` | 산식 버전 변경 건수 |

결정: MVP부터 DORA 지표와 운영 큐 지표를 함께 수집한다.

인당 생산성/마진 기여: 개발 생산성과 운영 처리량을 동시에 보아 릴리즈 속도가 실제 업무 효율로 이어지는지 판단한다.

PM 승인 필요: SLO 목표값, 보안 임계값, 최종 메트릭 저장소.

결정: 영업이익 KPI는 시스템 메트릭이 아니라 업무 이벤트 기반 지표로 수집하고, 산식/비율 변경은 감사로그와 설정 변경 로그에 남긴다.

인당 생산성/마진 기여: 낮은 영업이익 원인을 할인, 반품, 배송비, 처리시간, 결재 지연으로 분해해 즉시 개선 대상을 찾는다.

PM 승인 필요: 영업이익 MVP 산식 최종 확정, 원가/판관비 비율 변경 권한, KPI 대시보드 상용 도구.

## 11. 알림 기준

| 등급 | 조건 | 채널 | 대응 |
|---|---|---|---|
| P1 Critical | 인증 불가, 전체 장애, 개인정보 로그 저장 실패, 정산/포인트 대량 오류 | 즉시 알림, 전화/메신저 병행 가능 | 온콜 즉시 대응, PM 보고 |
| P2 High | 핵심 API 오류율/지연 임계값 초과, 운영 큐 급증, 외부 연동 실패 지속, 배송 지연/오배송/반송 급증, 비용 입력 실패/차단 급증 | 업무시간 즉시 알림 | 담당 에이전트 확인, 임시 우회 |
| P3 Medium | 배치 지연, 일부 고객사 오류, 배포 후 경고 증가, 택배사 API 부분 실패, 재조회 큐 적체 | 채널 알림 | 다음 업무 주기 내 처리 |
| P4 Low | 용량 증가, 회전 예정, 비용 이상 징후 | 대시보드/일일 리포트 | 계획 반영 |

필수 알림 라우팅:

- CRM Core 장애: A1, A2, A9
- Operations Console 장애: A2, A3, A8, A9
- Security & Audit 장애: A7, A9, PM 보고 필요
- Profitability Dashboard 장애: A3, A4, A9
- 배송/입출고 장애: A3, A6, A9
- Integration 장애: A6, A9

결정: 알림은 시스템 임계값만이 아니라 업무 영향도 기준으로 등급화한다.

인당 생산성/마진 기여: 모든 경고를 같은 수준으로 처리하지 않아 고비용 장애에 먼저 대응하고 알림 피로를 줄인다.

PM 승인 필요: 온콜 운영 방식, 알림 채널, 야간 대응 기준, 고객 공지 기준, 상용 알림 도구, 택배사 연동 계약/API/비용.

## 12. 운영 KPI와 관측성 연결

| 운영 KPI | 연결 시스템 지표 | 연결 로그/이벤트 | 활용 |
|---|---|---|---|
| 담당자 1인당 처리 건수 | `operation_task_completed_count`, `queue_depth` | 업무 상태 변경 로그 | 인력 배치와 자동화 후보 선정 |
| 평균 처리시간 | `operation_task_duration_p50/p95` | 접수, 배정, 완료 이벤트 | 병목 업무 유형 식별 |
| SLA 준수율 | `sla_breach_count`, `task_age` | SLA 초과 이벤트 | 고객사별 운영 리스크 관리 |
| 정산/포인트 오류율 | `settlement_error_rate`, `point_job_failure_rate` | 정산/포인트 변경 감사로그 | 재무 리스크와 문의 비용 감소 |
| 외부 연동 실패율 | `integration_failure_rate`, `retry_count` | 외부 API 요청/응답 로그 | 재처리 자동화와 공급사/연동사 협의 근거 |
| 배송/입출고 실패율 | `delivery_failure_rate`, `carrier_integration_failure_rate`, `manual_waybill_correction_count` | 배송 상태 변경, 택배사 API 실패, 수동 송장 입력 이벤트 | 배송 문의와 반품/지연 손실 감소 |
| 고객사/공급사 마진 | `profitability_refresh_lag`, `data_pipeline_success_rate` | 원가/매출 데이터 적재 이벤트 | 저마진 계정 조정 판단 |
| 영업이익 및 영업이익률 | `sales_amount`, `cost_amount`, `sgna_amount`, `operating_profit`, `operating_profit_rate` | 매출인식, 원가/판관비 산식 적용, 산식 변경 로그 | 낮은 이익 원인 분석과 가격/할인 정책 조정 |
| 이익 저하 원인 | `discount_amount`, `return_amount`, `delivery_cost_amount`, `exception_handling_time_ms`, `approval_delay_ms` | 할인, 반품, 배송비, 예외 처리, 결재 지연 이벤트 | 마진 훼손 업무와 고객/상품 식별 |
| 개인정보 접근 이상 | `pii_access_count`, `privileged_access_count` | 개인정보 조회 감사로그 | ISMS 대응과 내부통제 |
| 권한/설정 변경 추적 | `permission_change_count`, `settings_change_count` | 권한 변경, 메뉴 설정, KPI 산식 설정, 기능 플래그 변경 로그 | 내부통제와 책임소재 확인 |
| 수익성 비용 입력 추적 | `cost_input_change_count`, `cost_input_denied_count`, `cost_input_failure_count` | 제원원가, 판관비, 일반관리비 입력/수정/삭제, 권한 차단 로그 | 비용 입력 오류와 권한 오남용 식별 |
| 산식 버전 변경 추적 | `formula_version_change_count` | 산식 버전 생성, 원가율 변경, 판관비/일반관리비율 변경, 적용일 변경 로그 | 영업이익 지표 신뢰도와 변경 책임 추적 |
| 메뉴별 로그 수집 | `menu_log_event_count` | 메뉴 진입, 조회, 생성, 수정, 삭제, 결재, 반출, 권한 차단 로그 | 메뉴별 오류/권한/반출 이슈 식별 |
| 로그 조회 감사 | `log_view_count`, `log_export_request_count` | 로그 목록 조회, 상세 조회, 반출 요청 로그 | 로그 오남용 방지와 외감 증적 신뢰성 확보 |
| 입사등록 결재 관측성 | `onboarding_approval_event_count`, `onboarding_approval_lead_time_ms` | 입사등록 생성, 상신, 승인, 반려, 보류, 최종 승인 반영 로그 | 입사 처리 병목과 개인정보 생성 통제 확인 |
| 배포 안정성 | `change_failure_rate`, `mttr`, `deployment_frequency` | 배포/롤백 이벤트 | 릴리즈 병목 및 품질 개선 |

결정: KPI 대시보드는 업무 이벤트, 감사로그, 시스템 메트릭을 같은 `tenant_id`, `entity_id`, `trace_id`, `time_window` 기준으로 연결한다.

인당 생산성/마진 기여: 장애나 지연이 처리량, SLA, 마진에 미치는 영향을 빠르게 계산해 감이 아니라 데이터로 인력과 우선순위를 조정한다.

PM 승인 필요: 경영 KPI 공식 정의, 마진 계산 원천 데이터, 데이터 접근 권한.

## 13. MVP 인수조건

| 항목 | 인수조건 |
|---|---|
| 목적 | P0 모듈을 안전하게 개발, 검증, 배포, 관측할 수 있는 기준이 문서화되어 있다. |
| 입력 | PRD P0 모듈, 프로세스 요구사항, 규제 요구, 외부 연동 가정, 미확정 승인 항목이 반영되어 있다. |
| 출력 | CI/CD, 환경, 시크릿, 로그, 메트릭, 알림, KPI 연결 기준이 산출되어 있다. |
| 한 명령 기준 | `make setup`, `make lint`, `make test`, `make build`, `make deploy`, `make rollback` 계약이 존재한다. |
| 환경 분리 | local/dev/staging/prod 목적, 데이터, 접근, 배포 기준이 정의되어 있다. |
| 시크릿 | 저장 금지, 주입, 접근, 회전, 감사, 노출 대응 기준이 정의되어 있다. |
| 관측성 | 로그 필드, 감사 이벤트, 시스템/보안 메트릭, 알림 등급이 정의되어 있다. |
| KPI 연결 | 운영 KPI, 영업이익 KPI, 배송/연동 실패 지표와 시스템 지표의 매핑이 정의되어 있다. |
| 보존 기준 | 타임로그, 감사로그, 배포 이력, 외감 대응 증적의 보존 요구가 정의되어 있다. |
| 변경 추적 | 권한 변경, 설정 변경, 영업이익 산식 변경이 감사로그 대상에 포함되어 있다. |
| 비용 입력 관측성 | `cost_input_type`, `formula_version`, `actor_role`, `before_ref`, `after_ref`, `approval_id`, `result`가 비용 입력 로그 필드에 포함되어 있다. |
| 비용 입력 알림 | 비용 입력 실패, 권한 차단, 산식 버전 변경 급증이 알림 후보에 포함되어 있다. |
| 메뉴/로그 조회 | 메뉴별 로그 필드와 로그 조회 자체의 감사로그 기준이 포함되어 있다. |
| 입사등록 결재 | 입사등록 결재 상태 변경과 결재라인 로그가 관측성 대상에 포함되어 있다. |
| 승인 표시 | 특정 클라우드/상용도구/개인정보/외부계약 확정 항목은 PM 승인 필요로 표시되어 있다. |

인당 생산성/마진 기여: 인수조건을 명확히 해 에이전트 간 재해석 비용과 통합 지연을 줄인다.

## 14. 의존성

| 의존성 | 담당 | 상태 |
|---|---|---|
| 아키텍처 경계와 런타임 후보 | A1, 팀장 | PM 승인 필요 |
| 데이터 모델과 KPI 원천 | A4 | 미확정 |
| 개인정보 흐름도와 보관 기준 | A7 | PM 승인 필요 |
| 외부 연동 API와 장애 정책 | A6 | 미확정 |
| 택배사 API, 송장번호 체계, 배송 계약 조건 | A6, PM | PM 승인 필요 |
| 운영 업무 상태와 SLA 정의 | A2, A3, A8 | 미확정 |
| 유통 프로세스 타임로그 대상과 영업이익 산식 | A4, A7, PM | PM 승인 필요 |
| 수익성 비용 입력 권한과 산식 버전 정책 | A4, A7, A9, PM | PM 승인 필요 |
| 릴리즈 승인 권한 | PM 김권일 | PM 승인 필요 |

인당 생산성/마진 기여: 의존성을 명시해 병렬 작업 중 막히는 지점을 줄이고 승인 대기 비용을 관리한다.

## 15. 담당

| 역할 | 책임 |
|---|---|
| A9 DevOps | CI/CD, 환경, 시크릿, 관측성, 로그 보존, 배포 이력 기준 작성 |
| A1 Architecture | 아키텍처, 런타임, 서비스 경계 제공 |
| A2 Backend | API, 업무 이벤트, 감사로그 발생 지점 구현 기준 제공 |
| A3 Product/Frontend | 운영 콘솔과 KPI 대시보드 노출 요구 제공 |
| A4 Data | 수익성 지표, 데이터 파이프라인, KPI 계산 기준 제공 |
| A6 Integration | 외부 연동 실패, 재시도, 멱등성 기준 제공 |
| A7 Security | 개인정보, RBAC, 감사, ISMS 기준 제공 |
| A8 Automation | 자동화/RPA 처리량과 예외 큐 기준 제공 |
| PM 김권일 | 예산, 외부계약, 개인정보 처리범위, 최종 승인 |

인당 생산성/마진 기여: 책임 경계를 분리해 의사결정 대기와 중복 설계를 줄인다.

## 16. PM 승인 필요 목록

1. 특정 클라우드, 컨테이너 플랫폼, 배포 런타임.
2. CI/CD SaaS 또는 자체 구축 도구.
3. Secret Store, KMS, 키 회전 정책.
4. 로그/메트릭/트레이스 저장소와 상용 관측성 도구.
5. 로그 보존기간, 개인정보 마스킹 정책, 외감 증적 보존 기준.
6. 운영 SLO, 알림 채널, 온콜 운영 방식.
7. 운영 유사 데이터의 마스킹 사용 범위.
8. 개인정보/민감정보 처리범위, 보관기간, 위탁/재위탁 범위.
9. 배포 승인자와 릴리즈 캘린더.
10. 경영 KPI 공식 정의와 마진 계산 원천 데이터 접근 권한.
11. 영업이익 MVP 산식, 원가율/판관비율 변경 권한, 슈퍼바이저 공개 범위.
12. 택배사 실시간 위치 연동 계약, API, 비용, 장애 공지 기준.
13. 타임로그/감사로그 불변 저장 방식과 상용 로그 보관 도구.
14. 제원원가의 정확한 회계 의미, 비용 입력 권한 범위, 비용 입력 변경 결재 필요 여부.
15. 수익성 비용 입력 로그 보존기간, 무결성 검증 방식, 로그 반출 통제 기준.

인당 생산성/마진 기여: 승인 필요 항목을 분리해 미확정 전제로 인한 과잉 구축과 계약 비용을 방지한다.
